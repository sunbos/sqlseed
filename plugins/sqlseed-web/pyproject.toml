[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "sqlseed-web"
dynamic = ["version"]
description = "Local sqlseed workbench for SQLite and PostgreSQL rules, previews, generation and optional AI suggestions"
readme = "README.md"
requires-python = ">=3.10"
license = "AGPL-3.0-or-later"
license-files = ["LICENSE"]
authors = [{ name = "sqlseed contributors" }]
keywords = ["sqlseed", "test-data", "web-ui", "fastapi"]
dependencies = [
    "sqlseed>=0.2.4.dev0,<0.3",
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "pyyaml>=6.0",
    "packaging>=23.2",
]

[project.optional-dependencies]
ai = ["sqlseed-ai>=0.2.4.dev0,<0.3"]
dev = ["pytest>=8.0", "httpx>=0.27"]

[project.scripts]
sqlseed-web = "sqlseed_web.app:main"

[project.urls]
Homepage = "https://github.com/sunbos/sqlseed"
Repository = "https://github.com/sunbos/sqlseed/tree/main/plugins/sqlseed-web"
Documentation = "https://sunbos.github.io/sqlseed/web-workbench/"

[tool.hatch.build.targets.wheel]
packages = ["src/sqlseed_web"]

[tool.hatch.version]
source = "vcs"
fallback-version = "0.0.0"

[tool.hatch.version.raw-options]
root = "../.."

[tool.ruff]
line-length = 120

[tool.mypy]
strict = true
