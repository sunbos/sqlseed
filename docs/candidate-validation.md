# 候选版本验收

候选分支：`codex/workbench-candidate`。先通过 Pull Request 验证同一提交，再决定是否合入 `main`。候选包用于试用，不等于正式 Release，也不自动发布到 PyPI。

## 获取可追溯的安装包

打开该 PR 的 CI 工作流。`packages` 任务会构建五个 package 的 sdist 和 wheel，执行依赖一致性检查，并在完整五包、仅 Core/Web 两种独立环境中启动真实 Web worker、验证页面和数据生成。成功后上传 `sqlseed-candidate-<commit SHA>`，保留 14 天。

下载时核对产物名称、PR 当前提交及 CI 所属提交。五个 wheel 应来自同一个产物；在新建的 Python 环境中一起安装，避免混用开发版与软件源中的旧插件。保留这组包和提交 SHA，反馈问题时同时提供 Python、操作系统、数据库版本及最小复现步骤。

## 实际试用范围

使用新建测试数据库或可丢弃的副本，依次验证：

1. 仅 Core/Web 启动：手动规则、预览、生成、保存配置及运行记录可用，缺失 AI 等扩展有明确提示。
2. 完整五包：AI 配置状态与实际连通性分别验证；真实模型的建议质量单独记录，不能用固定响应测试代替。
3. SQLite 与 PostgreSQL：连接、结构读取、父子表生成、数据库约束、分页读取及失败后的实际提交量。
4. 支持环境中的组件变更：卸载后对应功能提示不可用，设置和草稿保留；重新安装后恢复。Windows、系统 Python 或外部托管环境不支持页面内改包。
5. 服务器 Python：普通运行不要求本机 virtualenv；外部部署需提供认证与访问控制，页面不能选择任意远端解释器。详见[Web 工作台](web-workbench.md)。

## 合并条件与当前限制

- 同一候选提交的 lint、类型、架构、文档、前端、Linux Python 矩阵、macOS/Windows、真实 PostgreSQL 和安装包 CI 均应通过。
- PR 的首次 CI 发现 Windows 文件 URI 身份规范化缺陷，以及 Linux SQLite URI 默认行为、Windows UTF-8 报告读取两处测试差异。修复后须重跑相同平台矩阵，不用本机通过替代 Windows/Linux 结果。
- SonarCloud 的可靠性与安全性门禁仍有待处理。已修复示例数据库固定公共临时路径，第二次实际分析也确认测试目录分类生效。其余误报与构建用途告警应按源码证据复核，不直接关闭规则或排除生产代码；最终状态以当前提交的分析为准。
- 本地完整离线验证明确排除依赖真实 LLM 的用例；远程未配置模型时的跳过也不代表模型质量已验收。
- 项目要求本地 mutation 门禁通过。完整运行的原始退出码、各类计数、源码身份及幸存复核须一并记录在 PR 中；新增容量契约用例的定向通过不能替代完整运行。
- 本轮幸存复核涉及诊断文本、等效分支及合法采样差异；不通过断言无关日志、锁死采样常数或放宽门禁来制造通过结果。该门禁结论未处理前，PR 保持 Draft，不自动合并。
- PostgreSQL 当前不支持 Web 清空后生成、复合外键和跨 schema 外键；Web 尚无多用户认证。完整支持边界见[支持与维护约定](maintainable-release.md)。

已发现的问题在候选分支修复并重新验证；本次用户标注仍须逐项验收，不能以自动测试通过代替未完成的视觉检查。每次修改源码后，使用新提交的 CI 和安装包验证，不混用之前的通过记录。

## 2026-09-12 复核进展

- 修复下拉标签误触；共享滚动条样式，模态窗口关闭时恢复背景滚动位置。实际浏览器复测仍待浏览器工具连接恢复。
- 独立 Web 服务实际卸载/安装 Mimesis 后恢复原连接，四表行数保持 12/8/24/48；缺失期间预览返回组件标识和安装/更换引擎提示。HTTP 验证不替代按钮和页面恢复的视觉验收。
- 修复整数/CHECK 和字符串硬长度容量规划；列域校验先于清表，enrich+self-FK 从清空后的数据读取引用。此调整不把整个 fill 变成覆盖后续 provider/插件错误的事务。
- SQLite 显式转换文本列中的日期，保留 SQLAlchemy 日期列处理器；AI 参数修复与 Core 共享别名和平铺输入规范化，避免非法参数在白名单之后被重新引入。
- Python 3.13 全量覆盖率复验：3290 passed、10 skipped、0 warnings；资源泄漏、弃用及不可引发异常警告均按错误处理。650 项 Node 回归通过。真实 Gemma 文件单独运行 7 项通过且无警告。跳过的 LM Studio、密钥和 Pillow 条件仍未覆盖。
- 远程覆盖率测试曾暴露 795 条 SQLite 未关闭连接警告；补齐测试资源释放，并修复 AI 外键扫描的实际连接泄漏。CI 现在将资源泄漏及不可引发异常警告按错误处理；新的远程结果须重新核对。
- 本地完整原始 mutation：239 total、231 killed、8 survived、退出码 2，无 timeout/suspicious/skipped/untested。6 项诊断文字、2 项合法但可能改变输出的采样差异仍待处理；没有放宽门禁或添加无关断言，PR 保持 Draft。
- CI 外部依赖采用版本/hash 锁、锁定构建工具和两项源码包例外；维护说明见仓库的 `.github/DEPENDENCIES.md`。新的远程检查结果以对应提交为准，不能沿用上一提交的绿色状态。
