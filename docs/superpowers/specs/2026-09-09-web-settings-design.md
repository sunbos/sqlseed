# Web 应用设置与预览稳定性

用户已确认上一轮评审结论，本次直接实施，保留既有未提交改动。

## 范围

主导航增加「设置」，内部「AI 服务」「插件与版本」两个分区。服务配置与安装信息属于运行 Web 的环境；生成引擎、语言、数量、字段规则继续属于生成配置。保留工作台显著的 AI 配置助手入口。

AI 助手只承担分析范围、业务描述、进度和建议审阅，展示当前服务与模型摘要。进入设置时仅在内存保存范围和业务描述，返回原配置且身份、epoch 和 schema 均有效时恢复助手。不得自动分析或恢复旧建议。

## 保存和检测

backend/model/base_url 持久化到应用数据目录，允许 SQLSEED_WEB_SETTINGS_PATH 覆盖。密钥仅来自环境变量或当前服务会话，不写磁盘、生成 YAML、运行记录、URL 或 localStorage。展示普通设置与密钥各自的来源和有效期。跨服务不能继承旧密钥。

检测当前表单草稿不保存设置；模型列表检测成功不等于完成模型推理。保存成功后只影响后续分析。配置或凭据改变后检测结果失效。保存/检测有在途反馈和重复提交保护，离页响应不能修改新页面。

插件页展示当前环境的 core/CLI/Web/AI/MCP 版本与可用状态，Base/Faker/Mimesis 单独分组。只读检查，不自动安装、升级或调用模型。Ollama 服务类型不直接标成纯本地推理。

## 预览稳定性

已有结果刷新时保留旧表、所选表和滚动，显示正在更新且当前为上次结果。成功替换有效结果，失败保留旧结果并解释原因。首次加载使用有限占位，不给短内容永久添加大面积空白。保留配置失效、连接变化与关闭生命周期保护。

## 验收

临时设置文件和 SQLite 隔离真实用户数据；测试持久化、凭据边界、检测与保存分离、未安装插件状态、AI 往返、预览刷新成功/失败/失效。运行 Web Python/Node 回归、lint、mypy、模块链接检查，并用浏览器检查独立设置页、窄屏、往返与预览布局。
